CREATE TABLE "public"."private_stream_visibility" ("profile_id" bigint NOT NULL, "view_profile_id" bigint NOT NULL, "created_at" timestamptz NOT NULL DEFAULT now(), PRIMARY KEY ("profile_id","view_profile_id") , FOREIGN KEY ("profile_id") REFERENCES "public"."profiles"("id") ON UPDATE cascade ON DELETE cascade, FOREIGN KEY ("view_profile_id") REFERENCES "public"."profiles"("id") ON UPDATE cascade ON DELETE cascade, UNIQUE ("profile_id", "view_profile_id"));
