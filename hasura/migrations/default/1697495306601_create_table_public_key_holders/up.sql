CREATE TABLE "public"."key_holders" ("address" citext NOT NULL, "subject" citext NOT NULL, "amount" integer NOT NULL, "updated_at" timestamptz NOT NULL DEFAULT now(), PRIMARY KEY ("address","subject") , UNIQUE ("address", "subject"));COMMENT ON TABLE "public"."key_holders" IS E'tracks the amount of keys an address holds in a given subject. updated with data from the key_tx table';
