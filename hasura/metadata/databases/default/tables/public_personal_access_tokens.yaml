table:
  schema: public
  name: personal_access_tokens
object_relationships:
  - name: profile
    using:
      manual_configuration:
        remote_table:
          schema: public
          name: profiles
        insertion_order: null
        column_mapping:
          tokenable_id: id
