table:
  name: profile_with_addresses
  schema: farcaster
object_relationships:
  - name: fids
    using:
      manual_configuration:
        column_mapping:
          fid: fid
        insertion_order: null
        remote_table:
          name: fids
          schema: farcaster
select_permissions:
  - role: user
    permission:
      columns:
        - fid
        - verified_addresses
        - avatar_url
        - bio
        - display_name
        - fname
        - updated_at
      filter: {}
      limit: 100
    comment: ""
